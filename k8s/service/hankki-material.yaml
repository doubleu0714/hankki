apiVersion: v1
kind: Service
metadata:
  name: hankki-material
spec:
  selector:
    app: hankki-material
  type: NodePort
  ports:
    - name: http
      port: 8081
      protocol: TCP
      targetPort: 8081
